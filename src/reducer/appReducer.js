const defaultState = {
  userName: "sabari1",
};

const appReducer = (state = defaultState) => {
  const key = "";
  switch (key) {
    default:
      return state;
  }
};

export default appReducer;
